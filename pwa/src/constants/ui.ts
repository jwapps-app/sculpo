// Matches the transform gizmo arrow colors: X red, Y green, Z blue. Used
// anywhere the UI labels an axis so they're visually tied to the arrows.
export const AXIS_COLORS = ["#dc2626", "#16a34a", "#2563eb"] as const;
